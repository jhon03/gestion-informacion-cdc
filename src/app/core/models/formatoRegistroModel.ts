export interface RecordFormat {
    idFormato: string;
    colaborador: string;
    nombreFormato: string;
    formato: any; // Usamos 'any' para permitir un tipo flexible que puede ser cualquier cosa
  }